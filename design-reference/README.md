# design-reference

Static HTML/CSS/JS mockups for visual comparison only.

Do **not** import these files, or code copied from them, into `apps/backend` or
`apps/storefront`. They exist so implementation work can be checked against the
intended look and feel — treat them as pictures, not source.

Current mockups:
- `mowy-homepage.html`
- `mowy-mystery-pull.html`
- `mowy-admin.html`
